#include "pinhole.h"

namespace SensorModel {

    // Lift 3d point in camera frame on normalized plane.
    void Pinhole::LiftToNormalizedPlane(const Vec3 p_c, Vec2 &norm_xy) {

    }

    // Lift 2d point in normalized plane back on camera frame.
    void Pinhole::LiftBackToCameraFrame(const Vec2 norm_xy, Vec3 &p_c) {

    }

}